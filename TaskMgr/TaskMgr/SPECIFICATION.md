# Спецификация TaskMgr

## 1. Назначение

TaskMgr - учебный веб-прототип для управления группами, расписанием, домашними заданиями, событиями недели, заметками и комментариями.

Сервер на Bottle отдает шаблоны и статические файлы. Данные прототипа сохраняются в браузере через `localStorage` и `sessionStorage`.

## 2. Технологии

- Backend: Python, Bottle.
- Шаблоны: `views/*.tpl`.
- Стили: обычный CSS в `static/content/css/`.
- Клиентская логика: `static/scripts/week-calendar.js`.
- Сборка стилей: не используется.

SCSS/Sass в проекте отсутствует.

## 3. Структура CSS

HTML подключает только четыре CSS-точки входа:

| Файл | Страница | Назначение |
| --- | --- | --- |
| `static/content/css/auth-reg/preview.css` | `/` | Импорт стилей стартовой страницы |
| `static/content/css/auth-reg/auth-reg.css` | `/reg-auth` | Импорт стилей входа и регистрации |
| `static/content/css/main/main.css` | `/main` | Импорт стилей главного экрана |
| `static/content/css/orders/orders.css` | `/orders` | Импорт стилей страницы заказов |
| `static/content/css/active-users/active-users.css` | `/active-users` | Импорт стилей страницы активных пользователей |

Каждая точка входа содержит `@import` маленьких файлов.

### 3.1 Стартовая страница

| Папка или файл | Назначение |
| --- | --- |
| `auth-reg/preview/base.css` | Сброс, переменные, базовая типографика |
| `auth-reg/preview/header.css` | Верхняя панель, навигация, логотип |
| `auth-reg/preview/buttons.css` | Кнопки лендинга |
| `auth-reg/preview/hero.css` | Hero-блок |
| `auth-reg/preview/sections.css` | Общие секции и панели |
| `auth-reg/preview/cards.css` | Карточки возможностей |
| `auth-reg/preview/dashboard.css` | Демонстрационные задачи, платформы, CTA |
| `auth-reg/preview/footer.css` | Подвал |
| `auth-reg/responsive/preview-responsive.css` | Адаптив стартовой страницы |

### 3.2 Вход и регистрация

| Папка или файл | Назначение |
| --- | --- |
| `auth-reg/base/auth-reset.css` | Базовые правила страницы входа |
| `auth-reg/auth/auth-layout.css` | Сетка страницы |
| `auth-reg/auth/auth-brand.css` | Левая брендовая область |
| `auth-reg/auth/auth-card.css` | Карточка формы и вкладки |
| `auth-reg/auth/auth-form.css` | Поля, кнопки, сообщения |
| `auth-reg/responsive/auth-responsive.css` | Адаптив страницы входа |

### 3.3 Главный экран

| Папка или файл | Назначение |
| --- | --- |
| `main/base/reset.css` | Сброс и базовые HTML-элементы |
| `main/base/theme.css` | Цвета, общие текстовые состояния |
| `main/layout/app-shell.css` | Трехколоночный layout |
| `main/components/buttons.css` | Кнопки |
| `main/components/sidebar.css` | Профиль, группа, заметки |
| `main/components/workspace.css` | Заголовок, роль, пустое состояние |
| `main/components/calendar.css` | Неделя, дни, карточки событий |
| `main/components/details.css` | Правая панель, комментарии |
| `main/components/forms.css` | Поля форм |
| `main/components/modal.css` | Модальные окна |
| `main/responsive/main-responsive.css` | Адаптив главного экрана |

### 3.4 Оформленные заказы

| Файл | Назначение |
| --- | --- |
| `orders/base.css` | Базовые стили страницы заказов |
| `orders/header.css` | Верхняя навигация |
| `orders/layout.css` | Hero-блок и сетка страницы |
| `orders/form.css` | Форма добавления заказа и ошибки |
| `orders/list.css` | Карточки заказов |
| `orders/responsive.css` | Адаптив страницы |

### 3.5 Активные пользователи

| Файл | Назначение |
| --- | --- |
| `active-users/base.css` | Базовые стили страницы активных пользователей |
| `active-users/header.css` | Верхняя навигация |
| `active-users/layout.css` | Hero-блок и сетка страницы |
| `active-users/form.css` | Форма добавления пользователя и ошибки |
| `active-users/list.css` | Карточки пользователей |
| `active-users/responsive.css` | Адаптив страницы |

## 4. Маршруты

| Маршрут | Шаблон | Назначение |
| --- | --- | --- |
| `/` | `views/index.tpl` | Стартовая страница |
| `/reg-auth` | `views/reg-auth.tpl` | Вход и регистрация |
| `/main` | `views/main.tpl` | Рабочее пространство группы |
| `/orders` | `views/orders.tpl` | Оформленные заказы |
| `/active-users` | `views/active-users.tpl` | Активные пользователи |
| `/static/<filepath:path>` | `static/` | Статические файлы |

## 5. Роли

- Гость: видит стартовую страницу и может перейти к входу.
- Авторизованный пользователь: может создать группу или войти по коду.
- Староста `leader`: создает и редактирует события группы.
- Ученик `member`: просматривает события и оставляет комментарии.

## 6. Функциональность

- Регистрация сохраняет пользователя в `localStorage.taskmgr_users`.
- Вход создает `localStorage.taskmgr_session`.
- Создание группы назначает создателю роль `leader`.
- Присоединение по коду назначает роль `member`.
- События недели хранятся внутри группы.
- Староста может добавлять и редактировать события.
- Ученик может комментировать события.
- Заметки сохраняются отдельно для каждого пользователя.
- Страница `/orders` доступна только авторизованному пользователю.
- Страница `/orders` выводит из `data/orders.json` только заказы текущего пользователя.
- Форма `/orders` добавляет новый заказ после серверной проверки.
- Новый заказ сохраняется с идентификатором и именем владельца.
- При ошибках поля формы сохраняют введенные значения.
- После успешного добавления форма очищается через redirect.
- Страница `/active-users` выводит активных пользователей, выбранных из общего списка `data/active_users.json` по рейтингу.
- Форма `/active-users` добавляет нового пользователя после серверной проверки.

## 6.1 Оформленные заказы

Объект заказа содержит:

- номер заказа;
- автора или клиента;
- описание;
- дату в формате `YYYY-MM-DD`;
- телефон в формате `+7XXXXXXXXXX`, `8XXXXXXXXXX` или с разделителями.
- `owner_id` пользователя, создавшего заказ;
- `owner_name` отображаемое имя пользователя.

Проверки:

- номер обязателен, уникален и содержит 3-20 допустимых символов;
- автор обязателен и не короче 2 символов;
- описание обязательно и не короче 10 символов;
- дата обязательна, соответствует формату и не позже текущей даты;
- телефон обязателен и содержит корректный российский номер.

Заказы сортируются по дате от новых к старым. Перед выводом список фильтруется по `owner_id`, который берется из cookie текущей сессии.

## 6.2 Активные пользователи

Объект пользователя содержит:

- ник;
- описание;
- дату активности в формате `YYYY-MM-DD`;
- телефон в формате `+7XXXXXXXXXX`, `8XXXXXXXXXX` или с разделителями;
- количество созданных событий;
- количество комментариев;
- количество заметок;
- количество групп.

Проверки:

- ник обязателен, уникален и содержит 3-24 допустимых символа;
- описание обязательно и не короче 10 символов;
- дата активности обязательна, соответствует формату и не позже текущей даты;
- телефон обязателен и содержит корректный российский номер.
- все метрики активности обязательны и должны быть неотрицательными целыми числами.

Рейтинг активности считается по формуле:

```text
события * 4 + комментарии * 2 + заметки + группы * 3 + бонус свежей даты
```

Активными считаются пользователи с рейтингом от `10` баллов. Пользователи сортируются по рейтингу активности от большего к меньшему, затем по дате активности.

## 7. Хранение данных

| Ключ | Описание |
| --- | --- |
| `localStorage.taskmgr_users` | Аккаунты |
| `localStorage.taskmgr_session` | Текущая сессия |
| `localStorage.taskmgr_groups` | Группы, роли, события, комментарии |
| `localStorage.taskmgr_notes_<userId>` | Личные заметки |
| `localStorage.isAuth` | Флаг авторизации |
| `sessionStorage.tempAuth` | Временная сессия |
| `taskmgr_user_id` | Cookie с id пользователя для серверной проверки `/orders` |
| `taskmgr_username` | Cookie с логином пользователя |
| `taskmgr_display_name` | Cookie с отображаемым именем пользователя |
| `data/orders.json` | Файл заказов для страницы `/orders` |
| `data/active_users.json` | Файл существующих пользователей и метрик для страницы `/active-users` |

## 8. Ограничения

- Нет серверной базы данных.
- Пароли хранятся в браузере в открытом виде.
- Cookie авторизации в прототипе не подписаны серверным секретом.
- Проверка ролей выполняется на клиенте.
- Нет удаления событий.
- Нет переключения между несколькими группами.

## 9. Критерии приемки

- `/` загружает `preview.css` и все его импортируемые файлы без 404.
- `/reg-auth` загружает `auth-reg.css` и все его импортируемые файлы без 404.
- `/main` загружает `main.css` и все его импортируемые файлы без 404.
- `/orders` загружает `orders.css` и все его импортируемые файлы без 404.
- `/orders` перенаправляет неавторизованного пользователя на `/reg-auth`.
- `/orders` показывает только заказы текущего пользователя.
- `/orders` добавляет валидный заказ в `data/orders.json`.
- `/orders` показывает ошибки валидации без очистки формы.
- `/active-users` загружает `active-users.css` и все его импортируемые файлы без 404.
- `/active-users` добавляет валидного пользователя в `data/active_users.json`.
- `/active-users` показывает ошибки валидации без очистки формы.
- Unit-тесты проверки даты и телефона выполняются через `python -m unittest discover -s tests`.
- В проекте нет папки `static/content/scss`.
- В `package.json` нет Sass-зависимости и SCSS-скриптов.
